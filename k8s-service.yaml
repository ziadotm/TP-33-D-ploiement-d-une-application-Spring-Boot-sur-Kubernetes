apiVersion: v1
kind: Service
metadata:
  name: cloud-platform-service
  namespace: cloud-platform
  labels:
    app: cloud-microservice
    version: "2.0.0"
    author: medori42
spec:
  type: NodePort
  selector:
    app: cloud-microservice
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30080
      protocol: TCP